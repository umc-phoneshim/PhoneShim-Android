/*
 * 이 파일은 삭제 대상입니다. (샌드박스에서 파일 삭제 권한이 없어 내용만 비워 둡니다)
 *
 * 사용 사유 입력 여부를 월 단위로 조회하는 엔드포인트가 서버에 없습니다.
 * /api/usage-logs/calendar 는 목표 달성일이라 용도가 달라서
 * ReportRepository.getAchievedDates 로 따로 옮겼습니다.
 */
package com.phoneshim.android.domain.usecase
